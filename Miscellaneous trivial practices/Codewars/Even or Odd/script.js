function positiveSum(arr) {
    const initial = 0;
    arr.reduce((previ, current) => {
        current > 0 ? previ + current initial
    }, initial
        )
  }

console.log(getCount('Pepe perez se va de viaje'))